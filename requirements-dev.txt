-r requirements.txt
pytest>=8.0
# Pillow renders the OpenPose pose-skeleton (keyframe._pose_skeleton, deferred-
# imported) which test_keyframe exercises on CPU. The GPU image gets it via
# deploy/requirements.txt; the test env needs it explicitly.
Pillow>=10.0
